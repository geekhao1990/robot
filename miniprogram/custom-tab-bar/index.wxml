<view class="tab-bar">
  <block wx:for="{{list}}" wx:key="text">
    <view
      class="tab-item"
      data-index="{{index}}"
      bindtap="switchTab"
    >
      <view class="tab-text {{selected === index ? 'active' : ''}}">
        {{item.text}}
      </view>
    </view>
  </block>
</view>
